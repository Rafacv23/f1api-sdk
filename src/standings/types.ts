export interface DriverStandings {
  classificationId: number
  driverId: string
  teamId: string
  points: number
  position: number
  wins: number | null
}

export interface ConstructorStandings {}
